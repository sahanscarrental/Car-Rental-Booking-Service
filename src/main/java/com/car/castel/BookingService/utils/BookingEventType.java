package com.car.castel.BookingService.utils;

/**
 * Booking event types for the customers
 */
public enum BookingEventType {
    BOOKING_CREATED,
    BOOKING_CANCELLED,
    VEHICLE_PICKED_UP,
    VEHICLE_DROP_OFF
}